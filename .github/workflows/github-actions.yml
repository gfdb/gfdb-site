on:
  pull_request:
    branches:
      - master
    types: [closed]
jobs:
  build:
    name: Build
    runs-on: ubuntu-20.04
    if: github.event.pull_request.merged == true
    steps:
    - name: trigger single Job
      uses: gfdb/gfdb-site@master
      with:
        url: "http://159.223.153.129:8080/"
        user: "gfdb"
        token: ${{ secrets.JENKINS }}
        job: "gfdb-site"
